# ─────────────────────────────────────────────────────────────────
# can-attendance — Environment variables
# Generated placeholders; replace Supabase values with real credentials.
# ─────────────────────────────────────────────────────────────────

# Environment / runtime
NODE_ENV=production
TZ=Asia/Bangkok

# Reverse proxy mode (Caddy, nginx, etc.)
REVERSE_PROXY=true

# HTTPS / TLS (STEP 12)
# Behind Caddy: HTTPS_ENABLED=false (Caddy handles TLS termination)
HTTPS_ENABLED=false
HTTPS_HSTS=true
HTTPS_KEY_PATH=
HTTPS_CERT_PATH=

# --- Reverse proxy / HTTPS (Caddy) ---
CADDY_DOMAIN=localhost
CADDY_EMAIL=admin@example.com

# --- Free HTTPS via tunnel (docker-compose.tunnel.yml) ---
# Cloudflare quick tunnel needs NO variables.
# Ngrok (docker-compose.ngrok.yml) needs these:
NGROK_AUTHTOKEN=
NGROK_DOMAIN=

# Service ports
STATIC_PORT=8080
EVIDENCE_PORT=3030
ATTENDANCE_PORT=3031

# Optional path overrides
STATIC_ROOT=
EVIDENCE_STORAGE_DIR=
EVIDENCE_DATA_DIR=

# Evidence storage service
EVIDENCE_MAX_BYTES=524288
EVIDENCE_ALLOWED_ORIGINS=*
EVIDENCE_ADMIN_TOKEN=EY1mXicDstplvT3MWRkOfS8wyg6CAj4x

# Attendance API service
ATTENDANCE_ALLOWED_ORIGINS=*
ATTENDANCE_ADMIN_TOKEN=6jN5uTom1VY3IQxM4nve72lOEUra0qzk

# Supabase (required — replace with real project credentials)
SUPABASE_URL=https://esnualorflkjcamlbcxy.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImVzbnVhbG9yZmxramNhbWxiY3h5Iiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc4OTIwMjUyNywiZXhwIjoyMTA0Nzc4NTI3fQ.7rDGpBoAiqdAbuQNsA6OIvObzz2YivAauJzxQFX00ik
EVIDENCE_BUCKET=evidence-photos
